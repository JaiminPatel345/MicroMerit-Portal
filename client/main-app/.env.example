# Backend API URL
VITE_BACKEND_URL=http://localhost:3000

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/
# 1. Create a new project or select existing one
# 2. Enable Google+ API
# 3. Go to Credentials → Create Credentials → OAuth 2.0 Client ID
# 4. Application type: Web application
# 5. Add authorized redirect URIs:
#    - http://localhost:3000/auth/learner/oauth/google/callback (for development)
#    - Your production URL/auth/learner/oauth/google/callback (for production)
# 6. Copy the Client ID and Client Secret below

# Note: These are typically configured on the backend, not the frontend
# The frontend only needs the VITE_BACKEND_URL to make OAuth requests
